# it is not advised to push env 
# Required for draft generation
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# Optional — enables Gemini embeddings for RAG + memory semantic search
GOOGLE_API_KEY=
GOOGLE_EMBEDDING_MODEL=gemini-embedding-001

# API server
API_HOST=0.0.0.0
API_PORT=8000
